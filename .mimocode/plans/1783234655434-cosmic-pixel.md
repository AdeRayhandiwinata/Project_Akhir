# Ubah Teks Abu-abu Menjadi Hitam - Semua Halaman

## Ringkasan
Ubah semua class `text-gray-800` dan `text-gray-500` menjadi `text-black` pada semua halaman dashboard.

## File yang diubah
1. `app/dashboard/page.tsx`
2. `app/dashboard/armada/page.tsx`
3. `app/dashboard/transaksi/page.tsx`
4. `app/dashboard pelanggan/page.tsx`

## Perubahan
Ganti `text-gray-800` → `text-black` dan `text-gray-500` → `text-black` di semua file.

## Verifikasi
- Jalankan `npm run dev` dan buka `/dashboard`, `/dashboard/armada`, `/dashboard/transaksi`, `/dashboard/pelanggan`
- Pastikan semua teks berwarna hitam
